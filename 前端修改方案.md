# 竞赛组队系统 — 前端修改方案

> 适用范围：`frontend/` 目录（Vue 3 + Vite + Pinia + Tailwind）
> 原则：每项修改独立可验收、小步提交；标注「需后端配合」的项需要前后端同步修改，其余仅改前端。
> 优先级定义：**P0** = 必修（真实缺陷，会报错或行为错误）｜**P1** = 高优（核心功能不可用/契约不匹配）｜**P2** = 中优（体验与健壮性）｜**P3** = 增强（对照设计文档补齐）

---

## 一、P0 必修项（真实缺陷）

### 1.1 队长看到"退出队伍"按钮，点击必然报错
| 项 | 内容 |
|---|---|
| 位置 | `src/views/TeamDetailView.vue` L17-19、L120-123 |
| 问题 | `isMember` 包含队长（队长本身是成员），`v-else` 分支让队长看到"退出队伍"；后端 `leave` 对队长返回 400"队长请先转让或解散队伍" |
| 方案 | 按钮区改为三分支：队长 →「管理队伍」；普通成员 →「退出队伍」；非成员 →「申请加入」。`isLeader` 已存在，直接复用 |
| 验收 | 队长登录后进入自己队伍详情，不出现"退出队伍"，出现"管理队伍" |

### 1.2 首页 `load()` 无 catch，接口失败页面空白且抛未捕获异常
| 项 | 内容 |
|---|---|
| 位置 | `src/views/HomeView.vue` L31-45 |
| 问题 | `Promise.all` 中任一请求失败（如后端未启动），异常直接抛到 `onMounted`，产生 unhandled rejection，页面只有空白区块，无任何错误提示 |
| 方案 | 参照 `TeamsView.vue` L43 的写法，补 `catch` + `toast.show(e)`；可考虑失败时展示重试按钮 |
| 验收 | 关闭后端刷新首页，出现 toast 错误提示，页面不白屏、控制台无未捕获异常 |

### 1.3 `auth` store 初始化 JSON.parse 无保护，数据损坏即白屏
| 项 | 内容 |
|---|---|
| 位置 | `src/stores/auth.js` L7 |
| 问题 | `JSON.parse(localStorage.getItem('cm_user') || 'null')` 遇到损坏 JSON 直接抛错，整个应用初始化失败白屏，且无法被任何 catch 捕获 |
| 方案 | 包 try/catch，失败回退 `null`；同理 `_persist` 写入也建议 try/catch |
| 验收 | 手动向 localStorage 写入非法 JSON 后刷新，应用正常进入登录页 |

---

## 二、P1 高优项（核心功能不可用 / 前后端契约不匹配）

### 2.1 赛事下拉用"分类名"当"赛事名"，创建与筛选语义错误
| 项 | 内容 |
|---|---|
| 位置 | `src/views/TeamsView.vue` L29（筛选）、L99-102（筛选下拉）、L138-141（创建表单下拉） |
| 问题 | 下拉选项来自 `eventApi.categories()`（分类名），但提交/匹配的 `team.event_name` 是**具体赛事名**。结果：①创建队伍把分类名存进 `event_name`；②赛事筛选 `t.event_name === 分类名` 永远匹配不上，**筛选功能实际是坏的** |
| 方案 | 下拉数据源改为 `eventApi.list()` 返回的具体赛事（取 `name`）；筛选按赛事名匹配。若产品上确实要按"分类"筛，则需后端支持（见 2.2 备注） |
| 验收 | 创建队伍时可从赛事列表中选到具体赛事；按赛事筛选能正确过滤出对应队伍 |

### 2.2 「已申请，等待审核」状态缺失（需后端配合）
| 项 | 内容 |
|---|---|
| 位置 | `src/views/TeamDetailView.vue` L32-42；后端 `backend/app/schemas/team.py` |
| 问题 | 申请成功后按钮仍是"申请加入"，可反复点击，第二次被后端 400 拒绝；UX 文档明确要求"已申请过的队伍：按钮变为'已申请，等待审核'" |
| 方案 | ①后端：`TeamDetail` 增加字段（如 `my_application_status: str = ""`），查询当前用户对该队伍的申请状态；②前端：按钮按 `isMember / 我是队长 / 已申请 / 队伍已满 / 未申请` 五态渲染，申请成功后本地置为"已申请" |
| 验收 | 申请后按钮变为"已申请，等待审核"且不可重复点击；刷新页面状态保持 |

### 2.3 队长管理能力缺失 + 满员不禁用申请
| 项 | 内容 |
|---|---|
| 位置 | `src/views/TeamDetailView.vue`；API 已就绪：`teamApi.update/remove/invite` |
| 问题 | ①队长无任何管理 UI（无法邀请成员/编辑/解散），`update/remove/invite` 接口已封装却无处调用；②`vacancies` 已算出但未用于禁用申请按钮；③UX 文档要求"队伍满员：申请入口变灰" |
| 方案 | ①队长入口弹出管理面板：编辑队伍（名称/描述/人数上限/标签，复用 `Modal.vue`）、解散队伍（二次确认）；②邀请成员：输入学号/用户搜索调用 `teamApi.invite`；③`vacancies <= 0` 时申请按钮禁用并提示"队伍已满" |
| 验收 | 队长可完成编辑/邀请/解散；满员队伍申请按钮置灰 |

### 2.4 "X支队伍参赛"标签永不显示（需后端配合）
| 项 | 内容 |
|---|---|
| 位置 | `src/views/HomeView.vue` L118；后端 `backend/app/schemas/event.py` |
| 问题 | 前端用 `event.teams_count`，后端 `EventOut`/`Event` 模型均无此字段，值恒为 undefined |
| 方案 | 二选一：①后端在 `EventOut` 增加 `teams_count`（聚合 `teams` 表按 `event_name` 统计）；②前端删除该展示逻辑 |
| 验收 | 赛事卡片正确显示参赛队伍数，或不再出现空标签 |

---

## 三、P2 中优项（体验与健壮性）

### 3.1 队伍列表改为后端参数过滤，去掉全量拉取+本地过滤
| 项 | 内容 |
|---|---|
| 位置 | `src/views/TeamsView.vue` L24-35、L40-42；后端 `backend/app/routers/teams.py` L57-83 已支持 `search/event/skill/grade` |
| 问题 | 前端 `teamApi.list({})` 全量拉取后内存过滤，队伍量大时首屏与筛选都卡 |
| 方案 | 筛选条件变化时携带 `search/event/skill/grade` 参数请求后端；同时注意 2.1 修正后 `event` 参数传赛事名 |
| 验收 | 切换筛选触发网络请求而非本地过滤；结果一致 |

### 3.2 首页搜索语义名不副实
| 项 | 内容 |
|---|---|
| 位置 | `src/views/HomeView.vue` L66（提示文案）、`src/views/TeamsView.vue` L27（过滤逻辑） |
| 问题 | 提示"搜索赛事、队伍或队友标签..."，实际只按队伍名/描述过滤 |
| 方案 | 二选一：①改提示文案为"搜索队伍名称或描述"；②扩展过滤逻辑纳入 `tags` 与赛事名（配合 3.1 由后端搜索，后端 search 目前也只查 name/desc，需同步扩展） |
| 验收 | 文案与实际行为一致 |

### 3.3 退出队伍防重复提交
| 项 | 内容 |
|---|---|
| 位置 | `src/views/TeamDetailView.vue` L44-52 |
| 问题 | `leaveTeam` 无 loading 态，双击触发两次请求，第二次 400 并 toast 报错 |
| 方案 | 增加 `leaving` 状态，请求期间禁用按钮；成功后跳转 `/teams`（现有逻辑） |
| 验收 | 双击只发一次请求 |

### 3.4 首页取队伍数据的写法统一
| 项 | 内容 |
|---|---|
| 位置 | `src/views/HomeView.vue` L41 `teamRes?.items || teamRes || []` |
| 问题 | 后端返回纯数组，`?.items` 是死代码；与 `TeamsView.vue` L42 的 `list || []` 不一致 |
| 方案 | 统一为 `list || []` |
| 验收 | 代码审查通过，行为不变 |

### 3.5 错误提示体系
| 项 | 内容 |
|---|---|
| 位置 | `src/api/index.js` L23、`src/stores/toast.js`、`src/App.vue` L37-41 |
| 问题 | ①成功/失败/警告共用同一 toast 样式，无类型区分与图标；②后端 422 校验错误的 `detail` 是数组，toast 会显示 `[object Object]` |
| 方案 | ①toast store 增加 `type`（success/error/info），App.vue 按类型渲染配色与图标；②`api/index.js` 错误提取时对数组类型做格式化（如取第一条 `msg`） |
| 验收 | 错误信息为可读文本；toast 有类型区分 |

---

## 四、P3 增强项（对照 UX/设计文档补齐）

### 4.1 个人中心补齐
| 项 | 内容 |
|---|---|
| 位置 | `src/views/ProfileView.vue` |
| 缺口 | ①无资料编辑入口（`authApi.updateMe` 已封装、后端 `PUT /users/me` 已支持）；②"综合评分"写死"—"（L85）；③参赛经历列表、队友评价列表缺失；④注销账号缺失 |
| 方案 | ①加"编辑资料"弹窗（姓名/学校/专业/年级/电话）；②综合评分若无数据源则改为展示其他有效统计或隐藏；③按 UX 文档补充经历与评价区块（需后端提供数据则标注待定）；④注销需二次确认 |
| 验收 | 资料可编辑并持久化；页面无写死占位 |

### 4.2 通知中心空状态文案
| 项 | 内容 |
|---|---|
| 位置 | `src/views/NotificationsView.vue` L113 |
| 方案 | 按 UX 文档补"组队动态会实时通知你"副文案 |

### 4.3 首页轮播数据化 + 赛事卡片可点击
| 项 | 内容 |
|---|---|
| 位置 | `src/views/HomeView.vue` L15-19（写死轮播）、L111-121（赛事卡片无点击） |
| 方案 | ①轮播数据改从后端赛事接口取（或至少集中到可配置常量文件）；②赛事卡片点击跳转组队广场并按该赛事筛选（配合 2.1 修正后的赛事筛选） |
| 验收 | 轮播与后端数据同步；点赛事卡片进入对应队伍列表 |

### 4.4 移动端响应式适配
| 项 | 内容 |
|---|---|
| 位置 | 全站：`Sidebar.vue`（固定 `w-60`）、各视图 `grid-cols-3/2` 硬编码 |
| 方案 | 设计规范要求"移动端响应式适配"。低成本起步：`grid` 改 `sm:`/`md:` 断点降列数；侧边栏加 `hidden md:flex` + 移动端顶栏/抽屉（若时间紧可先只降列数 + 表单 `max-w` 已相对 OK） |
| 验收 | 375px 宽下页面不横向溢出，核心操作可完成 |

### 4.5 路由体验细节
| 项 | 内容 |
|---|---|
| 位置 | `src/router/index.js`、`src/api/index.js` L21 |
| 方案 | ①加 `scrollBehavior`（返回列表时恢复滚动位置）；②路由 afterEach 更新 `document.title`（与 `App.vue` 的 `pageTitle` map 复用）；③401 跳转由 `window.location.href` 改为 `router.push`（需在非组件模块引入 router，注意循环依赖） |

### 4.6 Modal 无障碍与交互
| 项 | 内容 |
|---|---|
| 位置 | `src/components/Modal.vue` |
| 方案 | 加 `role="dialog"`/`aria-modal`、ESC 关闭、打开时锁定背景滚动；动画可加 `<transition>` |

### 4.7 表单内联校验与 a11y
| 项 | 内容 |
|---|---|
| 位置 | `src/views/LoginView.vue`、`src/views/TeamsView.vue`（创建队伍） |
| 方案 | ①注册页加确认密码、学号格式前端校验，错误内联显示（UX 文档要求）；②`label` 绑定对应 input 的 `id`，点击 label 可聚焦 |

### 4.8 字体本地化
| 项 | 内容 |
|---|---|
| 位置 | `frontend/index.html` L7-9 |
| 方案 | Google Fonts 直连在国内可能失败/卡顿；建议删除外链或下载 Inter 字体本地引入 |

---

## 五、代码质量（顺手优化，不影响行为）

| 项 | 位置 | 方案 |
|---|---|---|
| 常量重复 | `TeamsView.vue` L17 与 `ProfileView.vue` L11（`allSkillTags`）；`LoginView.vue` L16 与 `TeamsView.vue` L22（`grades`） | 抽到 `src/constants/` 公共文件 |
| 属性语义 | `ProfileView.vue` L12（`allAttrTags` 混入"周末有空"等时间属性，UI 文案却是"专业属性"） | 拆分或统一文案 |
| 组件拆分 | `HomeView.vue`（125 行含轮播/推荐/赛事） | 轮播、赛事区块拆子组件 |
| 渐变重复 | `TeamCard.vue` L10-15 与 `style.css` `.avatar` | 收敛到统一 token |

---

## 六、安全与性能提醒（前端层面）

| 项 | 现状 | 建议 |
|---|---|---|
| token 存储 | `localStorage` 明文（`stores/auth.js`） | XSS 可窃取；中期改 httpOnly cookie，短期可接受 |
| 输入长度 | 队伍名/描述/标签无 maxlength | 前端按后端列宽（如 128/2000）先行限制，避免 422 |
| 轮询 | `Sidebar.vue` L27 每 15s 拉未读数 | 页面 `visibilitychange` 隐藏时暂停 |
| 图片 | 无懒加载 | 头像渐显（UX 文档要求），可用 `loading="lazy"` 起步 |

---

## 七、实施顺序与验收清单

### 阶段一（P0，半天）：1.1 → 1.2 → 1.3
- [ ] 队长按钮三分支正确
- [ ] 首页接口失败有错误提示、无白屏
- [ ] localStorage 损坏不白屏

### 阶段二（P1，1-2 天，含后端配合项）：2.1 → 2.2 → 2.3 → 2.4
- [ ] 赛事下拉为具体赛事，筛选可用
- [ ] 申请后显示"已申请，等待审核"
- [ ] 队长可编辑/邀请/解散，满员禁申请
- [ ] 参赛队伍数显示或移除

### 阶段三（P2，1 天）：3.1 → 3.2 → 3.3 → 3.4 → 3.5
- [ ] 筛选走后端参数
- [ ] 搜索文案与行为一致
- [ ] 退出防重复
- [ ] 取数写法统一
- [ ] toast 类型化、错误信息可读

### 阶段四（P3，按需排期）：4.1 → 4.2 → 4.3 → 4.4 → 4.5 → 4.6 → 4.7 → 4.8
- [ ] 个人中心补齐
- [ ] 通知空状态文案
- [ ] 轮播数据化、赛事卡片可点击
- [ ] 移动端不溢出
- [ ] 滚动恢复、页面标题
- [ ] Modal 无障碍
- [ ] 表单内联校验
- [ ] 字体本地化

### 收尾
- [ ] 全流程回归：注册 → 登录 → 创建队伍 → 申请 → 队长接受 → 满员 → 退出/解散
- [ ] `npm run build` 通过

---

## 附：修改时不建议动的部分

- 整体配色 token 与设计规范已对齐，视觉层无需大改
- 后端路由/表结构除 2.2、2.4 的字段补充外无需变动
- 现有组件划分（Sidebar/Modal/TeamCard）合理，不必重构
